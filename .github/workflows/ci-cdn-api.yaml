name: CDN API CI

on:
  pull_request:
    branches: [main]
    paths:
      - 'apps/cdn-api/**' # * Triggers if files in this directory are changed
      - '.github/workflows/ci-cdn-api.yaml' # * Triggers if this file is changed

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./apps/cdn-api # Set default folder for commands to run in

    steps:
      - uses: actions/checkout@v6

      - name: Install Dependencies
        run: npm ci

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'
          cache: 'npm'
          cache-dependency-path: apps/cdn-api/package-lock.json

      - name: Lint
        run: npm run lint

      # Tests if exists
      # - name: Test
      #   run: npm run test

      - name: Built
        run: npm run build
